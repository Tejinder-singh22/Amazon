
//    AGENDA
// const Agenda = require("agenda");
// const agenda = new Agenda({ db: { address: "mongodb://localhost:27017/Socialmedia" } });
// agenda.define("send monthly billing report", async (job) => {
//    console.log(job);
// });
// (async function () {
//    // IIFE to give access to async/await
//    await agenda.start();
//  console.log("i am creating agenda ");
//    await agenda.every( "20 seconds","send monthly billing report");
//  })();


//    NODE CRON
   // var cron = require('node-cron');
   // cron.schedule('* * * * *', async () => { 
   // });


   //DIFFRENCE
//  agenda has more versions, more frequent updates, a bigger community of maintainers, more stars on Github, more followers on Github and more forks. node-cron has fewer dependencies and fewer open issues.



